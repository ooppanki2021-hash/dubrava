# Сайт — что здесь лежит

**Снимок обновлён 24.09.2026 до коммита `0c4c9e5266aeb7de10ec1f729f08e88bb2c7ced7`.**
Фактический путь от корня архива: `03-сайт/сайт_текущий/` (дефис, не подчёркивание).

## Состав

- **26 контентных страниц = главная + 25 тематических каталогов.** Не 26 каталогов плюс главная.
- `style.css`, изображения, favicon, конструктор и его ресурсы.
- `sitemap.xml` — **25 открытых URL**, `sitemap-full.xml` — **25** (с 24.09 без `/nabor/`).
- `robots.txt`; CNAME с привязкой домена, файлы верификации — сохранены.
- `app/` — точная опубликованная версия трекера, HTML 66 182 байта.
- Четыре Markdown-файла в этой папке — документация архива, а не копируемые документы сайта.

95 файлов взяты из исходного коммита (кроме его README). 94 HTTP-файла проверены побайтово; CNAME взят как настройка домена. Все пути и SHA-256 — в `../СВЕРКА_ПУБЛИКАЦИИ.json`.

## Новая версия трекера не потеряна

Она хранится в `../../05_приложение/app/`, HTML 80 825 байт; автономная копия — рядом. До сверки её копия была также внутри снимка сайта, из-за чего снимок не соответствовал публикации. Теперь роли разделены. Новая разработка сохранена побайтово, не откатывалась и не публиковалась.

## Читать перед правкой

- `ТЕХРЕГЛАМЕНТ_СТРАНИЦЫ.md` — структура и проверки.
- `КОНСТРУКТОР_как_устроен.md` — архитектура и известные ограничения, включая новый квиз.
- Постоянное резюме — текущее решение автора и границы доказательности.

## Про индексацию

25 открытых страниц — это разрешение индексации, не гарантия их присутствия в поиске. Основная карта включает все 25 открытых контентных URL. `/nabor/`, приложение и конструктор закрыты; два последних не входят в контентные 26.

`Sitemap-full` доступна по прямому адресу, но не указана как основная в robots.txt. Её отсутствие там не делает файл секретным. Старые sitemap-заготовки на 12 URL не применять.

## История прежних удалений — не инструкция

В записях 17–18.09 встречаются ранее удалённые папка `правки_залитые_15.09/`, `sitemap (1).xml` и заготовка на 12 URL. Эти сведения сохранены в истории Git и решениях; **в данном обновлении ни один исходный файл не удалялся**.

Будущие согласованные правки отдавать с точными путями. Не загружать целиком архив `dubrava` в репозиторий сайта.
