# Сайт — что здесь лежит

## ⭐ Читать до первой правки

- **`ТЕХРЕГЛАМЕНТ_СТРАНИЦЫ.md`** — каркас head/body, правила title и description, разрешённые CSS-классы, стандарт AUDIO, перелинковка, что нельзя трогать, чек-лист перед заливкой.
- **`КОНСТРУКТОР_как_устроен.md`** — архитектура `constructor-v5.html` (v7.5), три уже починенных бага, карта функций, регламент правки и тесты.

## сайт_текущий/ — слепок сайта

*Снят 15.09, `sitemap.xml` синхронизирован с живым 18.09.*

Клон рабочего репозитория **без** папки `.git`. Это ровно то, что сейчас на https://zapahstarosti.ru

- 26 папок страниц, в каждой `index.html`
- `index.html` в корне — главная
- `style.css` — все стили
- `sitemap.xml` (**7 URL — только открытые страницы**), `sitemap-full.xml` (26 URL — полный список, в robots.txt не указан)
- `robots.txt`
- `CNAME` — **не трогать**, привязка домена
- `yandex_e3e7231de3258122.html`, `googlebfe090009deb22b8.html` — верификация, **не удалять**
- `images/` — 26 картинок, ~3 МБ
- `constructor-v5.html` — конструктор страниц, вход `?admin=1`

**Как редактировать:** править файлы здесь, отдавать Инне архивом с сохранением структуры папок. Она заливает через GitHub → Add file → Upload files.

## правки_залитые_15.09/ — папка удалена 17.09

Все правки давно на сайте, дубли файлов только мешали. Что именно менялось — таблица в `01_НАЧНИ_ОТСЮДА/ИСТОРИЯ_РЕШЕНИЙ.md` (Решение 2), починка `/ubrat-zapah/` — в `01_НАЧНИ_ОТСЮДА/ПОЛНОЕ_ДОСЬЕ.md`. Сами файлы при нужде достаются из истории git.

## Про sitemap

`sitemap_новый_12url.xml` **удалён 18.09** — заготовка устарела: из 12 адресов 5 до сих пор под `noindex`.

**Правило теперь простое:** `sitemap.xml` содержит **только открытые страницы**. Открыли страницу —
перенесли её адрес из `sitemap-full.xml` в `sitemap.xml`, одним адресом. Никаких заготовок впрок.
`sitemap-week1.xml` в помине не было — упоминание было ошибкой в этом файле.
