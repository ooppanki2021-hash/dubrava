# Приложение-трекер «Шаги к мечте»

⭐ Техническое описание — в **`ПРИЛОЖЕНИЕ_как_устроено.md`**: хранение данных, вход по коду, пять вкладок, структура `DEFAULT_MILES`, функция `migrate()` (не удалять!), как добавить шаг, палитра.

Автономная копия `МЕЧТА_автономная_копия.html` — тот же файл целиком, работает офлайн.

PWA — устанавливается как обычная программа, работает офлайн.

## Файлы

| Файл | Что |
|---|---|
| `app/index.html` | Всё приложение в одном файле |
| `app/manifest.webmanifest` | Описание для установки |
| `app/sw.js` | Офлайн-кеш |
| `app/icon-192.png`, `icon-512.png`, `icon-180.png` | Иконки — годовые кольца дуба |

## Где живёт

Заливается в папку `app/` репозитория сайта → адрес **https://zapahstarosti.ru/app/**
Закрыто `noindex`, в поиск не попадёт.

## Код входа

**0820.** В файле хранится только хеш `25d74826` — сам код в исходнике отсутствует.

Если Инна забудет код: сгенерировать новый хеш функцией `pinHash()` из файла и подменить константу `PIN_DEFAULT_HASH`. Данные при этом не пропадут.

## Структура данных

Хранится в `localStorage` браузера, ключ `dubrava_dream_v1`:

```js
{
  tasks: [{id, d:'2026-09-15', t:'текст', n:'заметка', tag, rep, done}],
  miles: [{id, n:'название шага', d:'время и деньги', done, sub:[{t, done}]}],
  notes: [{id, date, text}]
}
```

**Путь из 20 шагов** зашит в константу `DEFAULT_MILES` в `index.html`. Там же 154 подшага. Редактировать прямо в файле.

⚠️ При изменении структуры `DEFAULT_MILES` работает функция `migrate()` — она переносит старые отметки на новые шаги по совпадению названий. Не удалять её.

## Как Инна передаёт своё состояние

Вкладка «Данные» → «Выгрузить состояние» → JSON-файл. По нему видно, что сделано и где она застряла.
